# 服务器状态分析报告

## 执行时间
2026-02-01

## 服务器基本信息

### 系统环境
- **操作系统**: OpenCloudOS
- **Node.js 版本**: v22.22.0
- **npm 版本**: 10.9.4

### Clawdbot/OpenClaw 安装情况

#### 版本信息
- **openclaw**: 2026.1.29 (a5b4d22)
- **clawdbot**: 2026.1.29
- **gateway**: 2026.1.29 (通过 `clawdbot gateway --version`)

#### 安装路径
```
/root/.nvm/versions/node/v22.22.0/bin/openclaw
```

#### 可用命令
- ✅ `openclaw` - 主命令
- ✅ `clawdbot` - 主命令（别名）
- ✅ `clawdbot gateway` - 网关命令
- ❌ `gateway` - 独立命令不存在

---

## 模型配置状态

### 已配置模型列表

| 模型名称 | 输入类型 | 上下文 | 本地 | 认证 | 标签 |
|---------|---------|-------|------|------|------|
| qwen-portal/vision-model | text+image | 125k | no | yes | default, configured |
| qwen-portal/coder-model | text | 125k | no | yes | configured, alias:qwen |
| anthropic/deepseek-v3.2 | - | - | - | - | configured, missing |

### 模型状态摘要

```
配置文件位置: ~/.openclaw/openclaw.json
Agent 目录: ~/.openclaw/agents/main/agent

默认模型: qwen-portal/vision-model
备用模型: 无 (0个)

图片模型: 未配置
图片备用模型: 无 (0个)

别名 (1个):
  qwen -> qwen-portal/coder-model

已配置模型 (3个):
  - qwen-portal/coder-model
  - qwen-portal/vision-model
  - deepseek-v3.2
```

---

## 认证状态

### 认证存储
```
认证存储位置: ~/.openclaw/agents/main/agent/auth-profiles.json
Shell 环境变量: 关闭
```

### OAuth/Token 提供商

#### qwen-portal (1个认证)
- **生效方式**: profiles:~/.openclaw/agents/main/agent/auth-profiles.json
- **配置文件数**: 1
  - OAuth: 1
  - Token: 0
  - API Key: 0
- **默认认证**: OAuth
- **模型配置**: models.json=qwen-oauth
- **来源**: ~/.openclaw/agents/main/agent/models.json

### OAuth/Token 状态

#### qwen-portal
- **qwen-portal:default**: ⚠️ **已过期** (expires in 0m)

---

## 问题分析

### 🔴 严重问题

1. **qwen-portal OAuth 认证已过期**
   - 状态: expired
   - 剩余时间: 0分钟
   - 影响: 无法使用 qwen-portal 相关模型

2. **deepseek-v3.2 模型配置不完整**
   - 状态: missing
   - 影响: 该模型无法正常使用

### 🟡 次要问题

1. **备用模型未配置**
   - 默认模型无备用
   - 图片模型无备用
   - 影响: 主模型故障时无法自动切换

2. **图片模型未配置**
   - 无默认图片模型
   - 影响: 无法处理图片相关任务

---

## 配置文件位置汇总

| 配置项 | 路径 |
|-------|------|
| 主配置文件 | ~/.openclaw/openclaw.json |
| Agent 目录 | ~/.openclaw/agents/main/agent |
| 认证配置 | ~/.openclaw/agents/main/agent/auth-profiles.json |
| 模型配置 | ~/.openclaw/agents/main/agent/models.json |

---

## 建议操作

### 优先级 1 - 紧急修复

#### 1. 更新 qwen-portal OAuth 认证

```bash
# 方法一：重新认证
openclaw auth login qwen-portal

# 方法二：使用 API Key（如果有）
export QWEN_API_KEY=your_api_key_here
```

#### 2. 修复 deepseek-v3.2 模型配置

检查配置文件，确保模型定义完整：

```bash
# 查看模型配置
cat ~/.openclaw/agents/main/agent/models.json

# 或使用 openclaw 命令
openclaw models show deepseek-v3.2
```

### 优先级 2 - 建议优化

#### 1. 配置备用模型

```bash
# 编辑配置文件添加备用模型
openclaw configure

# 或手动编辑 ~/.openclaw/openclaw.json
```

#### 2. 配置图片模型

```bash
# 如果需要图片处理功能，配置图片模型
openclaw configure
```

### 优先级 3 - 可选增强

#### 1. 启用 Shell 环境变量支持

```bash
# 编辑配置启用 shell env
openclaw configure
```

#### 2. 检查网关状态

```bash
# 检查网关是否运行
clawdbot gateway status

# 如果未运行，启动网关
clawdbot gateway --local --port 18789 --verbose
```

---

## 可用命令速查

### 模型管理
```bash
openclaw models list              # 列出所有模型
openclaw models status            # 查看模型状态
openclaw models show <model>      # 查看特定模型详情
```

### 认证管理
```bash
openclaw auth login <provider>    # 登录认证
openclaw auth status              # 查看认证状态
openclaw auth logout <provider>   # 登出认证
```

### 配置管理
```bash
openclaw configure                # 交互式配置
openclaw config show              # 显示当前配置
```

### 网关管理
```bash
clawdbot gateway --version        # 查看网关版本
clawdbot gateway --local --port 18789 --verbose  # 启动网关
clawdbot gateway stop             # 停止网关
```

---

## 环境变量建议

如果使用 API Key 而非 OAuth，建议配置以下环境变量：

```bash
# Qwen Portal
export QWEN_API_KEY=your_qwen_api_key

# OpenAI 兼容接口（如阿里百炼）
export OPENAI_API_KEY=your_openai_api_key
export OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Anthropic
export ANTHROPIC_API_KEY=your_anthropic_api_key
```

可以将这些变量添加到 `~/.bashrc` 或 `~/.openclaw/.env` 文件中。

---

## 总结

### 当前状态
- ✅ Clawdbot/OpenClaw 已正确安装（版本 2026.1.29）
- ✅ Node.js 环境正常（v22.22.0）
- ✅ 3个模型已配置（2个可用，1个缺失）
- ❌ qwen-portal OAuth 认证已过期
- ❌ deepseek-v3.2 模型配置不完整
- ⚠️ 无备用模型配置

### 下一步行动
1. **立即**: 更新 qwen-portal OAuth 认证
2. **立即**: 修复 deepseek-v3.2 模型配置
3. **建议**: 配置备用模型以提高可用性
4. **可选**: 配置图片模型以支持多模态任务

---

**报告生成时间**: 2026-02-01
**OpenClaw 版本**: 2026.1.29 (a5b4d22)
