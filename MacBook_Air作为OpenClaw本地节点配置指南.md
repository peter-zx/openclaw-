# MacBook Air 作为 OpenClaw 本地节点配置指南

## 📚 目录

1. [整体流程概览](#整体流程概览)
2. [准备工作](#准备工作)
3. [云端操作步骤](#云端操作步骤)
4. [本地操作步骤](#本地操作步骤)
5. [网络连接配置](#网络连接配置)
6. [权限授权配置](#权限授权配置)
7. [测试连接](#测试连接)
8. [常用功能](#常用功能)
9. [安全配置](#安全配置)
10. [故障排除](#故障排除)
11. [检查清单](#检查清单)

---

## 整体流程概览

### 这是什么？

简单来说，这个方案可以让您的**云服务器**和**MacBook Air**互相连接，实现跨设备协作。

**举个例子**：
- 您在云服务器上运行 AI 助手
- AI 助手需要查看您的 Mac 日程安排
- 通过这个方案，云服务器可以直接访问您的 Mac，读取日程信息

### 工作流程图

```
第一步：云端准备
├─ 确认 OpenClaw 已安装
└─ 启动 Gateway（网关）

第二步：本地准备（MacBook Air）
├─ 安装 Node.js
├─ 安装 OpenClaw Node
└─ 启动本地节点

第三步：配对连接
├─ 本地节点生成配对码
└─ 云端服务器批准配对

第四步：网络配置
├─ 安装 Tailscale（推荐）
└─ 建立安全连接

第五步：权限设置
├─ 云端授权本地节点权限
└─ 配置访问范围

第六步：测试使用
├─ 测试基础命令
└─ 测试各项功能
```

### 分工说明

| 操作 | 在哪里执行 | 说明 |
|-----|----------|------|
| **云端操作** | 云服务器 | 安装 OpenClaw、启动网关、批准配对、授权权限 |
| **本地操作** | MacBook Air | 安装工具、启动节点、生成配对码、配置网络 |

---

## 准备工作

### 您需要准备什么？

#### 1. 云端准备

您需要一台已经安装好 OpenClaw 的云服务器。

**如何检查**：
```bash
# 在云服务器上执行
openclaw --version
```

**如果显示版本号**（如 `2026.1.29`），说明已经安装好了。✅

**如果提示命令不存在**，需要先安装 OpenClaw。

---

#### 2. 本地准备（MacBook Air）

您需要准备：

- ✅ 一台 MacBook Air（推荐 macOS 12+）
- ✅ 管理员权限（可以安装软件）
- ✅ 稳定的网络连接
- ✅ 一个 Tailscale 账户（免费注册，后面会教）

---

#### 3. 必要信息

- ✅ 云服务器的 IP 地址或访问方式（SSH 或控制台）
- ✅ 云服务器的登录密码或密钥
- ✅ Tailscale 账户（https://tailscale.com/ 免费注册）

---

### 准备工作检查清单

在开始之前，请确认：

- [ ] 云服务器上已安装 OpenClaw
- [ ] 可以正常访问云服务器
- [ ] MacBook Air 可以正常上网
- [ ] 已注册 Tailscale 账户
- [ ] 了解基本的终端操作（复制粘贴命令）

---

## 云端操作步骤

### 第一步：确认 OpenClaw 已安装

在云服务器上执行以下命令：

```bash
openclaw --version
```

**预期结果**：
```
2026.1.29
```

✅ 如果看到版本号，说明已安装，可以跳到第二步。

❌ 如果提示命令不存在，需要先安装：

```bash
npm install -g openclaw@latest
```

---

### 第二步：启动 Gateway（网关）

Gateway 是 OpenClaw 的核心服务，负责管理所有节点。

```bash
# 启动网关
openclaw gateway start

# 查看网关状态
openclaw gateway status
```

**预期结果**：
```
Gateway is running
Status: online
Port: 18789
```

✅ 如果显示 `Gateway is running`，说明启动成功。

---

### 第三步：查看待配对节点

此时您的本地节点还未启动，所以列表应该是空的。

```bash
# 查看待配对的节点
openclaw nodes list --pending
```

**预期结果**：
```
No pending nodes found
```

这是正常的，等本地节点启动后，这里会显示您的 MacBook Air。

---

### 第四步：等待本地节点启动

**现在暂停云端操作，转到本地操作步骤！**

等您完成了本地配置后，再回到云端执行配对。

---

## 本地操作步骤

### 第一步：安装 Homebrew（包管理器）

Homebrew 是 macOS 上安装软件的工具，类似应用商店。

#### 1.1 打开终端

按 `Cmd + Space`，输入"终端"，按回车打开。

#### 1.2 安装 Homebrew

复制以下命令到终端，按回车执行：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**安装过程中**：
- 系统会提示输入密码，输入您的 Mac 登录密码（输入时不会显示字符）
- 按 `Enter` 键确认安装
- 等待 5-10 分钟（取决于网络速度）

#### 1.3 配置环境变量

安装完成后，需要告诉系统 Homebrew 在哪里：

```bash
# 添加到系统路径
echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.zshrc

# 重新加载配置
source ~/.zshrc

# 验证安装
brew --version
```

**预期结果**：
```
Homebrew 4.x.x
```

✅ 如果看到版本号，说明安装成功。

---

### 第二步：安装 Node.js

Node.js 是 OpenClaw Node 运行所需的环境。

```bash
# 安装 Node.js 22
brew install node@22

# 添加到系统路径
echo 'export PATH="/opt/homebrew/opt/node@22/bin:$PATH"' >> ~/.zshrc

# 重新加载配置
source ~/.zshrc

# 验证安装
node --version
npm --version
```

**预期结果**：
```
v22.x.x
10.x.x
```

✅ 如果看到版本号，说明安装成功。

---

### 第三步：安装 OpenClaw Node

OpenClaw Node 是让您的 Mac 成为节点的软件。

```bash
# 安装 OpenClaw Node
npm install -g openclaw-node@latest

# 验证安装
openclaw-node --version
```

**预期结果**：
```
openclaw-node v1.x.x
```

✅ 如果看到版本号，说明安装成功。

---

### 第四步：启动本地节点

#### 4.1 创建工作目录

```bash
# 创建专用目录
mkdir -p ~/openclaw-node

# 进入目录
cd ~/openclaw-node

# 查看当前目录（确认路径）
pwd
```

**预期结果**：
```
/Users/yourusername/openclaw-node
```

---

#### 4.2 初始化节点

```bash
# 初始化节点
openclaw-node init --name "MacBook-Air" --tags "mac,personal"
```

**参数说明**：
- `--name "MacBook-Air"`：给节点起个名字，方便识别
- `--tags "mac,personal"`：给节点打标签，便于管理

**预期结果**：
```
✅ Node initialized successfully
📁 Configuration directory: ~/openclaw-node
🔑 Node ID: mac-abc123-xyz789
📝 Config file: ~/openclaw-node/node.json
```

**重要提示**：
- 记下 **Node ID**（如：mac-abc123-xyz789），后面会用到

---

#### 4.3 启动节点

```bash
# 启动节点
openclaw-node start
```

**预期结果**：
```
🖥️ OpenClaw Node starting...
🔑 Node ID: mac-abc123-xyz789
📡 Pairing code: ABC-123-XYZ
🌐 Listening on ws://localhost:8080
⏳ Waiting for approval from gateway...
```

**⚠️ 非常重要**：
- ✅ **记下 Pairing code**（如：ABC-123-XYZ）
- ✅ **不要关闭这个终端窗口**
- ✅ **保持节点运行**

---

#### 4.4 后台运行（可选）

如果您希望节点在后台持续运行（关闭终端也不影响），使用以下命令：

```bash
# 按 Ctrl+C 停止当前节点
# 然后使用后台模式启动
nohup openclaw-node start > ~/openclaw-node/node.log 2>&1 &

# 查看日志
tail -f ~/openclaw-node/node.log
```

---

### 第五步：安装 Tailscale

Tailscale 用于建立安全的网络连接。

#### 5.1 安装 Tailscale

```bash
# 安装 Tailscale
brew install tailscale
```

#### 5.2 登录 Tailscale

```bash
# 登录（会自动打开浏览器）
tailscale up
```

**登录步骤**：
1. 命令执行后，浏览器会自动打开
2. 使用您的 Google、GitHub 或邮箱账户登录
3. 授权 Tailscale 访问
4. 登录成功后，返回终端

#### 5.3 获取 IP 地址

```bash
# 查看 Tailscale IP
tailscale ip -4
```

**预期结果**：
```
100.x.x.x
```

**重要提示**：
- ✅ **记下这个 IP 地址**（如：100.100.100.1）
- ✅ **后面云端配置会用到**

---

### 第六步：返回云端操作

**现在本地节点已经启动并生成了配对码，请回到云端操作步骤！**

您需要：
- 本地的 Pairing code（配对码）
- 本地的 Tailscale IP 地址

---

## 网络连接配置

### 云端操作：安装 Tailscale

在云服务器上执行：

```bash
# Ubuntu/Debian 系统
curl -fsSL https://tailscale.com/install.sh | sh

# 登录
tailscale up

# 查看连接状态
tailscale status
```

**预期结果**：
```
100.x.x.y your-server
100.x.x.x MacBook-Air
```

✅ 如果能看到您的 MacBook Air，说明 Tailscale 连接成功。

---

### 云端操作：批准配对

现在回到云服务器，执行配对：

```bash
# 查看待配对的节点
openclaw nodes list --pending
```

**预期结果**：
```
Pending Nodes:
┌──────────────────────┬──────────────────────┬──────────────┐
│ Node ID              │ Name                 │ Tags         │
├──────────────────────┼──────────────────────┼──────────────┤
│ mac-abc123-xyz789    │ MacBook-Air          │ mac,personal │
└──────────────────────┴──────────────────────┴──────────────┘
```

✅ 如果能看到您的 MacBook Air，说明本地节点已成功连接到云端。

---

### 云端操作：批准配对

使用之前在本地记下的 Pairing code：

```bash
# 批准配对（替换为您的 code）
openclaw nodes approve ABC-123-XYZ
```

**预期结果**：
```
✅ Node approved successfully
🔗 Node 'MacBook-Air' is now connected
📡 Status: online
```

✅ 如果看到这个提示，说明配对成功！

---

### 云端操作：更新节点地址

为了让云端能够通过 Tailscale 连接到本地节点，需要更新节点地址：

```bash
# 更新节点地址（替换为 Mac 的 Tailscale IP）
openclaw nodes update MacBook-Air --address "ws://100.x.x.x:8080"

# 查看节点状态
openclaw nodes describe MacBook-Air
```

**预期结果**：
```
Node Details:
Name: MacBook-Air
ID: mac-abc123-xyz789
Status: online
Address: ws://100.x.x.x:8080
```

✅ 如果看到状态为 `online`，说明一切正常！

---

## 权限授权配置

### 为什么需要权限？

出于安全考虑，默认情况下云端无法随意控制您的 Mac。您需要明确授权才能让云端执行特定操作。

### 云端操作：授权基础权限

#### 1. 文件系统权限

```bash
# 授权读取文件
openclaw nodes run MacBook-Air -- command="openclaw-node permissions add filesystem read"

# 授权写入文件
openclaw nodes run MacBook-Air -- command="openclaw-node permissions add filesystem write"
```

#### 2. 应用程序权限

```bash
# 授权启动应用程序
openclaw nodes run MacBook-Air -- command="openclaw-node permissions add apps launch"
```

#### 3. 浏览器权限

```bash
# 授权控制浏览器
openclaw nodes run MacBook-Air -- command="openclaw-node permissions add browser control"
```

#### 4. 通知权限

```bash
# 授权发送通知
openclaw nodes run MacBook-Air -- command="openclaw-node permissions add notification send"
```

---

## 测试连接

### 云端操作：基础测试

在云服务器上执行以下测试命令：

#### 测试 1：查看当前目录

```bash
openclaw nodes run MacBook-Air -- command="pwd"
```

**预期结果**：
```
/Users/yourusername
```

---

#### 测试 2：查看系统信息

```bash
openclaw nodes run MacBook-Air -- command="uname -a"
```

**预期结果**：
```
Darwin MacBook-Air.local 23.x.x Darwin Kernel Version x.x.x
```

---

#### 测试 3：查看文件列表

```bash
openclaw nodes run MacBook-Air -- command="ls -la ~/Documents"
```

**预期结果**：
```
total 0
drwx------+  5 yourusername  staff   160 Feb  2 00:00 Documents
-rw-r--r--   1 yourusername  staff  1234 Feb  1 10:00 file1.txt
-rw-r--r--   1 yourusername  staff  5678 Feb  1 11:00 file2.pdf
```

---

#### 测试 4：创建测试文件

```bash
openclaw nodes run MacBook-Air -- command="echo 'Hello from Cloud!' > ~/test.txt"

# 读取文件
openclaw nodes run MacBook-Air -- command="cat ~/test.txt"
```

**预期结果**：
```
Hello from Cloud!
```

---

#### 测试 5：删除测试文件

```bash
openclaw nodes run MacBook-Air -- command="rm ~/test.txt"
```

---

### 测试结果判断

✅ **如果所有测试都成功**：
- 说明云端和本地连接正常
- 权限配置正确
- 可以开始使用各项功能

❌ **如果测试失败**：
- 检查网络连接
- 检查节点状态
- 查看故障排除章节

---

## 常用功能

### 功能 1：查看 Mac 文件

在云服务器上执行：

```bash
# 查看 Documents 目录
openclaw nodes run MacBook-Air -- command="ls -la ~/Documents"

# 查看 Downloads 目录
openclaw nodes run MacBook-Air -- command="ls -la ~/Downloads"

# 查看 Desktop 目录
openclaw nodes run MacBook-Air -- command="ls -la ~/Desktop"
```

---

### 功能 2：读取 Mac 日程（需要安装插件）

#### 本地操作：安装 macos-tools 插件

在 MacBook Air 上执行：

```bash
# 安装插件
openclaw-node plugins install @openclaw/macos-tools

# 验证安装
openclaw-node plugins list
```

---

#### 云端操作：读取日程

在云服务器上执行：

```bash
# 读取未来 3 天的日程
openclaw nodes run MacBook-Air -- script="macos/read-calendar" -- args='{"days": 3}'
```

**预期结果**：
```json
{
  "events": [
    {
      "title": "团队会议",
      "date": "2026-02-03",
      "time": "10:00",
      "duration": "1 hour"
    }
  ]
}
```

---

### 功能 3：打开 Mac 应用

在云服务器上执行：

```bash
# 打开 Safari
openclaw nodes run MacBook-Air -- command="open -a Safari"

# 打开 Chrome
openclaw nodes run MacBook-Air -- command="open -a 'Google Chrome'"

# 打开邮件
openclaw nodes run MacBook-Air -- command="open -a Mail"

# 打开指定网址
openclaw nodes run MacBook-Air -- command="open https://www.baidu.com"
```

---

### 功能 4：发送通知到 Mac

在云服务器上执行：

```bash
# 发送通知
openclaw nodes run MacBook-Air -- command="osascript -e 'display notification \"来自云端的消息\" with title \"通知\"'"
```

**预期结果**：
- 您的 Mac 会弹出一个系统通知

---

### 功能 5：查看 Mac 系统信息

在云服务器上执行：

```bash
# 查看电池状态
openclaw nodes run MacBook-Air -- command="pmset -g batt"

# 查看磁盘使用
openclaw nodes run MacBook-Air -- command="df -h"

# 查看内存使用
openclaw nodes run MacBook-Air -- command="vm_stat"
```

---

## 安全配置

### 云端操作：设置访问限制

编辑配置文件：

```bash
nano ~/.openclaw/openclaw.json
```

添加安全配置：

```json
{
  "security": {
    "remoteAccess": {
      "requireAuth": true,
      "allowedCommands": [
        "filesystem.read",
        "filesystem.write",
        "apps.launch"
      ],
      "maxExecutionTime": 30000
    }
  }
}
```

保存后重启网关：

```bash
openclaw gateway restart
```

---

### 本地操作：设置访问白名单

在 MacBook Air 上执行：

```bash
# 只允许访问特定目录
openclaw-node config set security.allowedPaths ["~/Documents", "~/Downloads", "~/Desktop"]

# 只允许执行特定命令
openclaw-node config set security.allowedCommands ["ls", "pwd", "cat", "open"]

# 启用日志记录
openclaw-node config set logging.level "info"
```

---

## 故障排除

### 问题 1：节点无法启动

**症状**：执行 `openclaw-node start` 后没有响应

**解决方案**：

```bash
# 查看详细日志
openclaw-node start --verbose

# 检查端口是否被占用
lsof -i :8080

# 如果端口被占用，更换端口
openclaw-node config set port 8081
openclaw-node start
```

---

### 问题 2：无法配对节点

**症状**：在云端执行 `openclaw nodes approve` 失败

**解决方案**：

```bash
# 检查节点是否在线
openclaw nodes list --pending

# 检查 Tailscale 连接
tailscale status

# 测试网络连接
ping mac-tailscale-ip

# 重新启动节点
openclaw-node restart
```

---

### 问题 3：命令执行失败

**症状**：在云端执行命令时返回错误

**解决方案**：

```bash
# 检查权限
openclaw nodes run MacBook-Air -- command="pwd"

# 查看错误详情
openclaw nodes run MacBook-Air -- command="pwd" --verbose

# 查看节点日志
openclaw-node logs --tail 50
```

---

### 问题 4：Tailscale 连接失败

**症状**：无法通过 Tailscale 连接到 Mac

**解决方案**：

```bash
# 重新登录 Tailscale
tailscale up --reset

# 检查 IP 地址
tailscale ip -4

# 测试连接
ping mac-tailscale-ip
```

---

## 检查清单

### 云端检查清单

- [ ] OpenClaw 已安装
- [ ] Gateway 已启动
- [ ] Tailscale 已安装并登录
- [ ] 已批准节点配对
- [ ] 已更新节点地址
- [ ] 已授权基础权限
- [ ] 测试命令执行成功

---

### 本地检查清单

- [ ] Homebrew 已安装
- [ ] Node.js v22+ 已安装
- [ ] OpenClaw Node 已安装
- [ ] 节点已初始化
- [ ] 节点已启动
- [ ] 已记下 Pairing code
- [ ] Tailscale 已安装并登录
- [ ] 已记下 Tailscale IP
- [ ] 节点状态为 online

---

### 网络连接检查清单

- [ ] 云端和本地都在 Tailscale 网络中
- [ ] 可以互相 ping 通
- [ ] 端口 8080 未被占用
- [ ] 节点地址已更新

---

## 总结

### 核心步骤

1. **云端准备**：确认 OpenClaw 已安装，启动 Gateway
2. **本地准备**：安装 Node.js 和 OpenClaw Node，启动节点
3. **网络配置**：安装 Tailscale，建立安全连接
4. **配对连接**：云端批准本地节点配对
5. **权限设置**：云端授权本地节点权限
6. **测试使用**：验证各项功能正常

---

### 快速命令参考

**云端命令**：
```bash
openclaw --version                    # 查看版本
openclaw gateway start                # 启动网关
openclaw nodes list                   # 查看节点
openclaw nodes approve <code>         # 批准配对
openclaw nodes run MacBook-Air -- command="pwd"  # 执行命令
```

**本地命令**：
```bash
openclaw-node init --name "MacBook-Air"  # 初始化节点
openclaw-node start                      # 启动节点
openclaw-node status                     # 查看状态
openclaw-node logs                       # 查看日志
```

---

### 下一步建议

1. ✅ 完成基础配置
2. ✅ 测试基础功能
3. ✅ 探索高级功能
4. ✅ 配置安全策略
5. ✅ 自动化工作流

---

**文档版本**: 2.0
**最后更新**: 2026-02-02
**适用版本**: OpenClaw Node v1.x+

---

祝您配置顺利！如有任何问题，欢迎随时咨询。🚀
